const DB_USER = "test";
const DB_PASSWORD = "perragata27";
const DB_HOST = "cluster0.jdduep2.mongodb.net";

const API_VERSION = "v1";
const IP_SERVER = "localhost";

const JWT_SECRET_KEY = "nieladandi";

module.exports = {
    DB_USER,
    DB_PASSWORD,
    DB_HOST,
    API_VERSION,
    IP_SERVER,
    JWT_SECRET_KEY,
}